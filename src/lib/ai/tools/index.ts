export enum AppDefaultToolkit {
  WebSearch = "webSearch",
  Http = "http",
  Code = "code",
}

export enum DefaultToolName {
  WebSearch = "webSearch",
  WebContent = "webContent",
  Http = "http",

}
